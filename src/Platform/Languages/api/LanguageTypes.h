#ifndef QLOCKY_LANGUAGE_TYPES_H
#define QLOCKY_LANGUAGE_TYPES_H

/**
 * A list of all supported language types.
 * This enum is used to identify the language type in the application.
 * It is used in the LanguageServiceIfc interface to provide language-specific functionality.
 */
enum class LanguageTypes {
    DE_CH,
    EN_US,
};

#endif